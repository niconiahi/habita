services:
  go:
    image: ${GO_IMAGE:?GO_IMAGE required}
    env_file:
      - ../.env.prod
    environment:
      OTEL_ENVIRONMENT: production
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
          cpus: "0.25"
