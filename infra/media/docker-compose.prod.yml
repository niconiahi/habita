services:
  image:
    # Rootless in production (no custom certs needed)
    user: "1000:1000"
    env_file:
      - ../.env.prod
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "2.0"
        reservations:
          memory: 256M
          cpus: "0.5"
