# Observability subcommands — mirrors web app recipes for the observability app
# Usage: just observability db migrate, just observability lint types, etc.

mod db 'observability/db.just'
mod lint 'observability/lint.just'
