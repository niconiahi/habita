# Client error logging

This is a behind-the-scenes endpoint that the platform uses to send error logs from the user's browser to the central logging system. It's not something users interact with directly — it happens automatically when something goes wrong on the client side, helping the development team track and fix issues.
