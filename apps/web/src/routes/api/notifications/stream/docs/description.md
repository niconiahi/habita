# Notifications stream

This endpoint opens a real-time connection to push notifications to the browser as they happen. It's used by the notification bell in the header to show updates without requiring page refreshes.
