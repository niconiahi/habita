# OpenTelemetry proxy

An internal endpoint that forwards telemetry data (traces and logs) from the browser to the OpenTelemetry collector. It acts as a passthrough so the browser doesn't need direct access to the collector.
