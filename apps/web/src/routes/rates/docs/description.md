# Rate index management

This is an internal administration page only accessible to platform administrators (webmasters). It's used to manage the rate indexes that determine how rental prices are adjusted over time.

Administrators can view the current month's rates by type and update their values. If a rate for a given type and month doesn't exist yet, it gets created. If it already exists, it gets updated. These rates are then used when calculating rental price adjustments in contracts.
