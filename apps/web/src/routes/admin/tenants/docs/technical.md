# /admin/tenants

## Loader

Requires auth. Gets accessible property IDs (`LANDLORD` + `MANAGER`), then `fetch_tenants(property_ids)`.

## Actions

None — read-only page.

## Auth

Requires authenticated user with landlord or manager access.
