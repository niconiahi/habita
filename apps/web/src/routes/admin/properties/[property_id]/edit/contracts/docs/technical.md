# /admin/properties/[property_id]/edit/contracts

## Loader

No page-level loader. Contract data comes from parent layout (`property`).

## Actions

None — read-only page. Navigates to `/admin/properties/[property_id]/contracts/new` and `/admin/properties/[property_id]/contracts/[contract_id]/edit`.

## Auth

Inherited from parent layout (`require_edit_access()`).

## Key Components

Table, Button
