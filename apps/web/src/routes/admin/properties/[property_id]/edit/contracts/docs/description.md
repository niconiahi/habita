# Property contracts

This tab shows all contracts associated with the property. Managers can see each contract's state and end date, and click "Editar" to navigate to the contract editing page. There's also a "Crear contrato" button to create a new contract for this property.

This is a read-only view — actual contract editing happens in its own dedicated area.
