# /pricing

## Page (`+page.svelte`)

Static marketing page — no loader, no server file, no actions. Monthly/annual toggle is client-side only via `SegmentedButton`.

## Auth

None — public page.

## Key Components

SegmentedButton
