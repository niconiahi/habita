# /properties/[property_id]/tenant/services

## Loader

No page-level loader. Service data comes from parent layout (`services`).

## Actions

None — read-only page.

## Auth

Inherited from parent layout (tenant access).
