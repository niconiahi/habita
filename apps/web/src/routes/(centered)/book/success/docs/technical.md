# /book/success

## Page (`+page.svelte`)

Static confirmation page. Reads `date` from URL query param to display the booked visit time.

## Auth

None — public page.

## Notes

- `date` query param contains the scheduled visit date/time
- Links back to `/properties`
