# /digital_signature/rejected

## Page (`+page.svelte`)

Static page — no loader, no server file. Displays "Firma rechazada" message.

## Notes

- Callback URL from Alpha2000 Firmador when user rejects the signature
