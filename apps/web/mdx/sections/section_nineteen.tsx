import type { Props } from "~/lib/contract/pdf.server"

export function SectionNineteen({
  contract,
}: {
  contract: Props
}) {
  return `Se confiere al presente el carácter de título ejecutivo, no siendo necesario preparar la vía ejecutiva en caso de reclamo judicial de alquileres, pago de servicios, o cualquier otra obligación a cargo de "EL LOCATARIO".`
}
