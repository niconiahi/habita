.DS_Store
node_modules
.pnpm-store
.react-router
build
temp
plans
apps/legacy
.playwright-mcp

# Secrets: plaintext files are IGNORED
.env
infra/*/.env
infra/*/scheduler/rclone.conf

# Generated zone data (large, regenerate locally)
*.tsv

# Map tile data (large, download separately)
*.mbtiles

# Encrypted secrets (.enc) are TRACKED - do not add to gitignore
