#!/usr/bin/env bash
source "$(dirname "$0")/lib.sh"

LOCK_FILE="/tmp/habita-deploy.lock"

if [[ -f "$LOCK_FILE" ]]; then
  echo "Already locked ($(cat "$LOCK_FILE"))"
  exit 1
fi

echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) by $(whoami)" > "$LOCK_FILE"
echo "Locked."
