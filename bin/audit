#!/usr/bin/env bash
source "$(dirname "$0")/lib.sh"

LOG_FILE="$REPO_ROOT/infra/deploy.log"

if [[ ! -f "$LOG_FILE" ]]; then
  echo "no deploy history"
  exit 0
fi

echo "recent deploys:"
tail -20 "$LOG_FILE"
